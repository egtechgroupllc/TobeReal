import {COLORS, SIZES, WIDTH, FONTS, SHADOW, scale} from './theme';
import images from './images';
import animations from './animations';

export {COLORS, SIZES, WIDTH, FONTS, SHADOW, scale, images, animations};
